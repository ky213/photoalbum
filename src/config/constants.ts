/**
 * Specifies directoris where client will upload their photos.
 * PRIVATE is for client's private photos.
 * PUBLIC is for all static content.
 */

export const DIR = {
  PRIVATE: __dirname + "/../../uploads/private",
  PUBLIC: __dirname + "/../../uploads/public",
};
